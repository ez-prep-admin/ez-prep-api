{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": ".",
    "noEmit": true,
    "types": ["node", "jest", "multer", "express", "supertest"]
  },
  "include": ["src/**/*.spec.ts", "src/**/*.test.ts", "test/**/*.ts"],
  "exclude": ["node_modules", "dist"]
}
